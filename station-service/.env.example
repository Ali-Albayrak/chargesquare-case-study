# Station Service — local uvicorn (non-compose). Copy to .env and adjust.
# For docker compose, use root `.env.example` (DB host `db`, not localhost).
DATABASE_URL=postgresql+psycopg://chargesquare:chargesquare@localhost:5432/chargesquare
PORT=8001
