apiVersion: v1
kind: Service
metadata:
  name: station
  labels:
    app: station
spec:
  selector:
    app: station
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
